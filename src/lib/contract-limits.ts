/**
 * Όρια που ισχύουν για τον πελάτη στο portal.
 *
 * Ζουν σε ΞΕΧΩΡΙΣΤΟ αρχείο γιατί σε module με `"use server"` επιτρέπεται να
 * εξάγονται μόνο async συναρτήσεις — μια εξαγόμενη σταθερά σπάει ολόκληρο το
 * module κατά το build.
 *
 * Το portal δεν είναι ελεύθερη επεξεργασία της σύμβασης: οι πινακίδες
 * καθορίζουν ποιος μπαίνει στο πάρκινγκ και τι χρεώνεται.
 */

/** Πόσες πινακίδες επιτρέπονται ανά κλεισμένη θέση. */
export const MAX_PLATES_PER_SLOT = 3;

/** Πόσες αλλαγές ανά θέση επιτρέπονται μέσα στην ίδια σύμβαση. */
export const MAX_CHANGES_PER_SLOT = 2;

/**
 * Ελάχιστο διάστημα ανάμεσα σε δύο αλλαγές («όχι συνεχόμενα»).
 * Αν το σωστό διάστημα είναι άλλο, αλλάζει μόνο αυτή η σταθερά.
 */
export const CHANGE_COOLDOWN_HOURS = 24;
